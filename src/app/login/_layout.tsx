import SafeAreaView from "@/components/safearea";
import Layout from "@/screens/login/components/layout";

const LoginScreenLayout = () => (
  <SafeAreaView>
    <Layout />
  </SafeAreaView>
);

export default LoginScreenLayout;
