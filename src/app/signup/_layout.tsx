import SafeAreaView from "@/components/safearea";
import Layout from "@/screens/signup/components/layout";

const SignUpScreenLayout = () => (
  <SafeAreaView>
    <Layout />
  </SafeAreaView>
);

export default SignUpScreenLayout;
